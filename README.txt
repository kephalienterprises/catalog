To run the application:
1. Inside the catalog directory, start Git Bash
2. Enter the following commands:
	'vagrant up'
	'vagrant ssh'
	'cd /vagrant'	
	'python database_setup.py'
	'python application.py'
3. On host computer, navigate to localhost:1234